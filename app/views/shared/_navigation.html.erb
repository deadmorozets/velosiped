<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/orders">Заказы</a>

    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                      <li><a href="/people">Сотрудники</a></li>
                      <li><a href="/occupations">Должности</a></li>
                    </ul>
                    <%= image_tag("bicycle.png", size: '64x54') %>

                    <ul class="nav navbar-nav navbar-right">

                      <%= form_tag orders_search_path, method: :get, class: "navbar-form navbar-left",
                                                                      role: 'search' do %>
                        <div class="form-group">
                          <%= text_field_tag :query, params[:query], class: 'form-control',
                                                                      placeholder: 'Заказ содержит...' %>
                        </div>
                        <%= submit_tag 'Поиск', class: 'btn btn-default' %>
                      <% end %>


                    <% if false %>
                      <form class="navbar-form navbar-left" role="search">
                        <div class="form-group">
                          <input type="text" class="form-control" placeholder="Найти в заказах">
                        </div>
                        <button type="submit" class="btn btn-default">Поиск</button>
                      </form>
                    <% end %>

                      <% if user_signed_in? %>
                        <li><%= link_to 'Выйти', destroy_user_session_path, :method => :delete %></li>
                      <% else %>
                        <li><%= link_to 'Войти', new_user_session_path %></li>
                      <% end %>
                      <li><%# link_to 'Sign Up', new_user_registration_path %></li>
                    </ul>
    </div>
  </div>
</nav>